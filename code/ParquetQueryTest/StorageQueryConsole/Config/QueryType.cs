namespace StorageQueryConsole.Config
{
    public enum QueryType
    {
        Distinct,
        PointFilter,
        MaxBy,
        MinMax,
        FilterCount,
        TimeFilterCount,
        TotalCount
    }
}